<form method="post" action="{{ path('app_code_formation_delete', {'id': code_formation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ code_formation.id) }}">
    <button class="btn">Delete</button>
</form>
