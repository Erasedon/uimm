<form method="post" action="{{ path('app_stats_delete', {'id': stat.id}) }}" onsubmit="return confirm('êtes vous sur de vouloir supprimer cette element ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stat.id) }}">
    <button class="btn">Supprimer</button>
</form>
