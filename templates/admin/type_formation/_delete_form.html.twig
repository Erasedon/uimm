<form method="post" action="{{ path('app_type_formation_delete', {'id': type_formation.id}) }}" onsubmit="return confirm('êtes vous sur de vouloir supprimer cette element ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_formation.id) }}">
    <button class="btn">Supprimer</button>
</form>
