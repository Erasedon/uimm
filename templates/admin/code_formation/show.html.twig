{% extends 'base.html.twig' %}

{% block title %}CodeFormation{% endblock %}

{% block body %}
    <h1>CodeFormation</h1>

    <table class="min-w-full text-left text-sm font-light"  >
        <tbody>
            <tr>
                <th scope="col" class="px-6 py-4">Id</th>
                 <td class="whitespace-nowrap px-6 py-4" >{{ code_formation.id }}</td>
            </tr>
            <tr>
                <th scope="col" class="px-6 py-4">Code</th>
                 <td class="whitespace-nowrap px-6 py-4" >{{ code_formation.code }}</td>
            </tr>
            <tr>
                <th scope="col" class="px-6 py-4">Formation</th>
                 <td class="whitespace-nowrap px-6 py-4" > {% for formation in code_formation.formations %}
                        {{ formation.titre }}
                    {% endfor %}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_code_formation_index') }}">Retour vers la liste</a>

    <a href="{{ path('app_code_formation_edit', {'id': code_formation.id}) }}">Modifier</a>

    {{ include('admin/code_formation/_delete_form.html.twig') }}
{% endblock %}
