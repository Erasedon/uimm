{% extends 'base.html.twig' %}

{% block title %}Stats{% endblock %}

{% block body %}
    <h1>Stats</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stat.id }}</td>
            </tr>
            <tr>
                <th>Annee</th>
                <td>{{ stat.annee }}</td>
            </tr>
            <tr>
                <th>Taux</th>
                <td>{{ stat.taux }}</td>
            </tr>
             <tr>
                <th>Niveau</th>
                <td>{{ stat.obtenirFormation.titre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_stats_index') }}">Retour vers la liste</a>

    <a href="{{ path('app_stats_edit', {'id': stat.id}) }}">Modifier</a>

    {{ include('stats/_delete_form.html.twig') }}
{% endblock %}
